import 'package:flutter/material.dart';

class OverallAgenda extends StatelessWidget {
  const OverallAgenda({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Overall Agenda'),
      ),
      body: Container(
        child: const Text('Overall Agenda'),
      ),
    );
  }
}
